/**
 * Arquivo temporário até a API publicar o contrato completo.
 * Não adicione DTOs manualmente aqui. Execute `npm run api:types` para substituí-lo.
 */
export interface paths {}

export interface components {
  schemas: never
}
